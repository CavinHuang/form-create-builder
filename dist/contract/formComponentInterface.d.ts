import { CustomComponentInterface } from "./customComponentInterface";
export declare abstract class FormComponentInterface extends CustomComponentInterface {
    constructor($field: string, $title: any);
}
