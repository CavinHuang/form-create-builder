import { Form } from "../core/form";
export declare abstract class BootstrapInterface {
    /**
     * 初始化
     *
     * @param Form $form
     * @return void
     */
    abstract init(form: Form): void;
}
